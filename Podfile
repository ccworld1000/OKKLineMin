# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'OKKLineMin' do
  use_frameworks!

  # Pods for OKKLineMin
  pod 'CCSQLite'
  pod 'SnapKit', '~> 5.0.0'

end
